# 唱片跑道播放器 Demo｜项目执行 Prompt

你是一名资深产品设计师、交互动效设计师和前端工程师。请直接在当前项目目录中完成一个可现场演示的移动端 H5「唱片跑道播放器」。这是一个 24 小时黑客松项目，目前只剩约 4 小时，因此优先完成视觉完整、交互闭环、演示稳定的 MVP，不做真实社交后端。

## 一、先阅读和复用现有内容

开始编码前，必须完整检查以下本地文件，不要凭空重画已有素材：

- `唱片跑道素材/music_player_UI_sample.jpg`：播放器整体视觉与信息层级参考
- `唱片跑道素材/music_player_disk.png`：必须直接用于唱片外圈/跑道，不要用 Canvas 重新绘制一个替代品
- `唱片跑道素材/Runaway_Baby_Cover.jpeg`
- `唱片跑道素材/青春修炼手册_cover.PNG`
- `唱片跑道素材/star_crossing_night_cover.PNG`
- `songs/runaway-baby.mp3`
- `songs/runaway-baby.lrc`
- `songs/youth-training-manual.mp3`
- `songs/youth-training-manual.lrc`
- `songs/star-crossing-night.mp3`
- `songs/Star Crossing Night (feat. GALI).lrc`
- `KUGOU-design-guidelines.md`：颜色、字体、间距、圆角、按钮和动效规范
- `唱片跑道Demo.html`：只作为已有功能参考，允许重构或替换；不要照搬其中错误的跑步轨迹与临时视觉
- `唱片跑道产品逻辑.md`：若为空，以本 Prompt 为准

先输出一段非常简短的实施计划，然后立即编码，不要等待确认。

## 二、产品定义

产品名暂定「唱片跑道」。

一句话定位：把播放器的黑胶唱片变成一条好友共同奔跑的跑道，让听歌成为一种轻量、无负担、可被记录和分享的陪伴。

核心情绪不是竞技排名，而是“有人和我一起”。不要设计速度榜、淘汰榜或强比较。速度只是制造相遇和动画变化的手段，亲密度通过共同听歌时长、合跑圈数和击掌次数表达。

主标语可使用：

> 这一程，有人陪你跑。

## 三、MVP 范围与明确取舍

只做一个移动端单页播放器，不新增复杂路由。通过底部抽屉、浮层和 Demo 控制台承载辅助功能。

需要完整实现：

1. 三首歌曲切换、播放、暂停、进度同步和歌词滚动。
2. 开启/关闭陪跑。
3. 唱片跑道上 1–8 个小人的入场、跑步、相遇击掌、退场。
4. 三首歌曲各自的场景和彩蛋。
5. 好友信息卡、踢出操作。
6. 一个可折叠的 Demo 管理面板，用于模拟好友上线、下线和人数变化。
7. 陪跑周报/分享卡生成、预览与下载。
8. 移动端 390 × 844 为主要设计基准，并兼容 375 × 812。

本次不做：

- 真实登录、好友关系、网络邀请、在线状态同步和服务端数据库。
- 真实留言双向同步；只预留数据结构或入口，不要让它干扰主流程。
- 因“陪伴时长不够”自动让好友消失。MVP 中好友只会因 Demo 面板下线、用户主动踢出或关闭陪跑而退场。
- 自定义 BPM 滑杆。歌曲节奏由歌曲配置决定，避免把播放器做成调速工具。

## 四、技术要求

若当前目录还不是工程化项目，则使用：

- Vite
- React 18
- TypeScript
- CSS Modules 或结构清晰的普通 CSS
- `lucide-react` 提供通用 UI 图标

不要为这个单页 Demo 引入 Zustand、React Router、动画重型框架或后端服务。状态用 React hooks 即可。人物与场景动画优先使用 DOM + CSS transform + requestAnimationFrame；如选择 Canvas，必须保证人物可点击、可访问，并且唱片素材与封面仍是本地图片资源。

播放时间必须以 `HTMLAudioElement.currentTime` 为唯一时间源。暂停时以下内容必须同时暂停：

- 音频
- 进度条
- 歌词滚动
- 人物沿轨道移动
- 歌曲彩蛋时间轴

切歌后必须重置上一首歌的歌词状态、彩蛋状态和一次性事件。

三首歌的本地 MP3 和 LRC 已经齐全，必须直接接入，不再实现“模拟时间轴”或“演示音频待放入”状态。构建项目时将素材复制到 Vite 可访问的 `public/assets/audio/` 与 `public/assets/lyrics/`，不要改动原始 `songs/` 文件。公开资源统一使用下表中的安全目标文件名，并通过 `import.meta.env.BASE_URL` 拼接 URL，不要写本机绝对路径或硬编码站点域名。

素材映射、公开目标文件名与已知时长：

| 歌曲 | 原始音频 → 公开文件名 | 原始歌词 → 公开文件名 | 封面 | 约时长 |
|---|---|---|---|---:|
| Runaway Baby | `songs/runaway-baby.mp3` → `runaway-baby.mp3` | `songs/runaway-baby.lrc` → `runaway-baby.lrc` | `唱片跑道素材/Runaway_Baby_Cover.jpeg` | 02:28.48 |
| 青春修炼手册 | `songs/youth-training-manual.mp3` → `youth-training-manual.mp3` | `songs/youth-training-manual.lrc` → `youth-training-manual.lrc` | `唱片跑道素材/青春修炼手册_cover.PNG` | 04:23.11 |
| Star Crossing Night (feat. GALI) | `songs/star-crossing-night.mp3` → `star-crossing-night.mp3` | `songs/Star Crossing Night (feat. GALI).lrc` → `star-crossing-night.lrc` | `唱片跑道素材/star_crossing_night_cover.PNG` | 03:25.04 |

实现一个可靠的 LRC 解析器，至少支持 `[mm:ss.xx]`，忽略空歌词行并按时间排序。当前高亮歌词由 `audio.currentTime` 计算；切歌、拖动进度和暂停后都必须立即同步。不要把 LRC 全部硬编码进组件。

不要在代码里用“当前歌词字符串是否包含某句话”来触发彩蛋；场景触发使用独立的 `sceneCues` 时间配置。LRC 时间仅作为确定 cue 时间的依据。

### 静态部署要求

- 项目必须可以作为纯静态 H5 部署，不依赖后端、开发服务器代理、远程字体、远程音频或密钥。
- 在 `vite.config.ts` 中设置 `base: "./"`；所有运行时 `fetch` 的 LRC 和 MP3 地址使用 `import.meta.env.BASE_URL` 构造，确保部署到域名根路径或子目录都能加载。
- 图片优先通过模块 `import` 进入构建产物；音频和 LRC 放在 `public/assets/` 后使用安全英文文件名。
- 音频加载、歌词加载失败时显示非阻塞错误提示，同时保持切歌和其他界面操作可用。
- `npm run build` 后必须运行 `npm run preview` 检查生产构建，而不是只验证开发服务器。
- 刷新页面后仍能进入播放器；本项目没有路由，不需要 SPA rewrite 配置。
- 最终部署目录为 `dist/`。

## 五、视觉与布局

严格遵守 `KUGOU-design-guidelines.md`，整体气质清新、年轻、轻盈、沉浸。关键要求：

- 字体：`PingFang SC, -apple-system, BlinkMacSystemFont, "Helvetica Neue", sans-serif`
- 页面水平边距 20px。
- 品牌蓝优先使用 `#31B5ED` / `#169AF3`。
- 播放器背景使用规范中的冰蓝纵向渐变：
  `linear-gradient(180deg, #C9E5FD 0%, #EFF6FF 48%, #F0FBFD 100%)`
- 主要文字接近黑色，辅助文字采用透明度层级，不要全页面使用高饱和蓝。
- 卡片以 20px 圆角为主；底部大抽屉顶部圆角 30px；小标签使用胶囊圆角。
- 图标使用线性图标，不使用 Unicode 符号或 emoji 充当按钮图标。
- 可以在歌曲彩蛋中出现毕业帽、爱心、星光等图形，但请用 CSS/SVG/图形绘制，不要直接显示 emoji 字符。
- 不要堆叠大量玻璃态卡片。播放器主体应以音乐、唱片跑道、小人和歌词为视觉核心。

页面自上而下建议结构：

1. 顶部栏：返回、标题「唱片跑道」、更多。
2. 陪跑摘要胶囊：「4 人正在陪跑 · 本周 126 分钟」。
3. 唱片舞台：页面第一视觉中心，尽量接近参考图比例。
4. 当前歌词：突出一行，上一行和下一行弱化。
5. 歌曲名与歌手。
6. 进度条与时间。
7. 播控：上一首、播放/暂停、下一首；主播放键 56px 左右。
8. 陪跑开关、邀请、好友管理、生成周报等次级入口。

封面应裁成圆形，放在 `music_player_disk.png` 的透明中孔下层。唱片外圈可做很慢的自转，但人物必须沿屏幕固定的椭圆/圆形跑道运动，不能随唱片纹理一起被旋转。唱片自转与人物公转是两个独立动画层。

## 六、人物系统与正确轨迹

使用简洁、有辨识度的 2D 小人。若没有人物图片素材，可用 DOM/CSS/SVG 组合出头、身体、手臂和腿；每位好友通过衣服颜色、头像色和名字区分。用户本人要有明确但克制的「我」标识，不使用皇冠制造排名感。

默认展示 4 人（包含用户），最多 8 人。人物的通用状态为跑步。

轨道算法要求：

- 为人物分配 3 条相邻的同心跑道半径，避免全部重叠在同一条线上。
- 使用参数角 `θ` 计算位置：`x = cx + rx * cos(θ)`，`y = cy + ry * sin(θ)`。
- 每个人的基础速度由歌曲 BPM 映射，再叠加稳定的个人 pace 系数（建议 0.94–1.06），只为制造自然相遇，不代表关系远近。
- 根据人物所处的椭圆下半区/上半区调整 `scale`、`z-index` 和轻微透明度，形成前后空间感。
- 人物朝运动切线方向，腿部摆动频率与移动速度一致。不要出现身体倒着跑、围绕自身旋转或脚下滑的效果。
- 发生相遇时不要让两个小人长时间穿模。进入阈值后暂时吸附到可击掌距离，播放约 700ms 击掌动作和小粒子，再恢复各自速度。
- 同一对好友击掌至少设置 12 秒冷却，防止连续刷屏。

点击非本人的好友小人，打开好友信息卡，展示：

- 名字与在线状态
- 关系标识，如「跑友 · 认识 86 天」
- 本周同听分钟
- 合跑圈数
- 击掌次数
- 次要操作「请 TA 离开跑道」，点击后需二次确认并播放退场动画

入场动画：人物从唱片边缘外小跑进入对应跑道，伴随轻量蓝色粒子和“阿杰来陪你了”提示，约 800ms。

退场动画：人物减速、回头挥手，再向外跑出并淡出，约 900ms。不要瞬间消失。

关闭陪跑时，好友依次退场，用户本人可以留在唱片中心或舞台外；再次开启时好友按 120ms 间隔依次入场。

## 七、三首歌曲场景

将每首歌曲配置为独立数据：

```ts
type Track = {
  id: string;
  title: string;
  artist: string;
  cover: string;
  audioSrc: string;
  bpm: number;
  durationFallback: number;
  lyrics: Array<{ time: number; text: string }>;
  sceneCues: Array<{
    id: string;
    start: number;
    end: number;
    type: "relay" | "graduationWalk" | "leftRightMove" | "firstMeet";
  }>;
};
```

### 1. Runaway Baby

- 主要/通用展示场景。
- BPM 建议 128。
- 人物持续跑步，速度差制造自然追赶与击掌。
- 第一次接力 cue 使用 `00:29.25–00:37.95`，与首段副歌同步。也可以在 `01:19.07–01:27.73` 再触发一次，但同一播放周期最多两次。
- 在 cue 区间触发接力彩蛋：前后两人靠近、递出一根明显的小接力棒，接棒成功后短暂加速，并显示「默契接棒 +1」。
- 接力是短动画，不要改变播放器功能状态。

### 2. 青春修炼手册

- 进入歌曲时，小人头顶戴上统一但颜色略有区别的毕业帽。
- 大部分时间从跑步切换为校园散步/轻快踏步，整体节奏更轻。
- 左右手动作 cue 使用 LRC 中四次副歌的真实时间：
  - `00:03.33–00:12.17`
  - `01:22.07–01:30.03`
  - `02:39.49–02:47.58`
  - `03:29.20–03:37.58`
- cue 内人物先举左手、再举右手。动作要清楚、同步；每个 cue 在一次播放周期内只触发一次，拖动进度重新进入 cue 时允许正确恢复场景。
- 舞台可出现少量纸飞机或毕业彩带，控制数量，不遮挡人物和歌词。

### 3. star crossing night

- 进入该歌曲后，场上突出用户与一位指定好友，其他好友降低透明度或暂时在外圈慢跑，不要粗暴消失。
- 使用真实 LRC 时间 `01:21.95–01:28.45` 作为一见钟情 cue。
- cue 开始前约 3 秒可进入预备编排，使两人在 `01:21.95` 附近从跑道两侧恰好相遇；随后短暂停步、抬头看向对方，头顶出现手绘感爱心和轻微星光。
- 动画结束后恢复正常跑步。
- 不要使用夸张告白弹窗，保持“一见钟情彩蛋”的轻盈感。

所有 cue 必须集中配置在 `tracks.ts`，不要散落在动画组件中。用户拖动进度跨过 cue、从 cue 中间开始播放或切歌时，场景状态必须由当前时间重新推导，不能只依赖一次性的 `setTimeout`。

## 八、好友 Demo 管理面板

提供一个仅演示用的可折叠底部面板，入口可命名为「好友管理」。面板中至少有 8 个预设角色：

- 用户本人固定在线，不可删除。
- 其他角色展示头像色、名字、在线开关、当前是否在跑道。
- 上线且陪跑开启：播放入场动画并加入跑道。
- 下线：播放退场动画后移出。
- 达到 8 人时，禁用其余加入操作并提示「跑道已满，最多 8 人」。
- 提供「一键 2 人浪漫场景」「一键 4 人标准场景」「一键 8 人满员场景」三个 Demo 快捷按钮。

邀请按钮在 MVP 中打开一个模拟邀请浮层，明确标注「Demo」并提供“复制邀请文案”功能，不要伪装成真实链接已经发送成功。

## 九、分享传播与激励

分享入口命名为「生成陪跑周报」，比“陪跑卡”更有累计感。

周报卡片采用 9:16 海报比例，必须包含：

- 主标题：「这一周，有 3 位好友陪我跑」
- 唱片跑道当前画面快照或可重现的视觉
- 本周同听时长，例如「126 分钟」
- 合跑圈数，例如「42 圈」
- 默契击掌，例如「和阿杰击掌 8 次」
- 一条有情绪的总结，例如「最长的一程不是单曲循环，是有人一直没下线」
- 当前歌曲名与日期
- 产品标识「唱片跑道」和短标语「这一程，有人陪你跑」

实现预览、关闭和下载 PNG。生成过程中要等待本地图片加载完成，避免 Canvas 因跨域资源被污染。分享数据可以是 Demo 数据，但击掌次数和当前人数应与本次会话状态联动。

## 十、交互细节与可访问性

- 所有按钮至少 44 × 44px 点击区域。
- 按钮使用真实 `button` 元素并有 `aria-label`。
- 尊重 `prefers-reduced-motion`：降低人物移动、粒子和唱片自转，但保留功能状态变化。
- 抽屉和弹窗支持遮罩关闭、关闭按钮和 Escape。
- 图片必须有 alt，加载失败时显示规范中的浅色占位。
- 不允许使用 `alert()` 代替正式 UI。
- 所有重要操作提供 1.5–2.5 秒的非阻塞 Toast。
- 页面在 iPhone 安全区下不能遮挡底部按钮。

## 十一、验收清单

完成后必须逐项自测：

- [ ] 首屏使用了本地 `music_player_disk.png`，三首歌都使用了对应本地封面。
- [ ] 上一首/下一首能循环切换三首歌。
- [ ] 三个本地 MP3 都能真实播放，页面不依赖远程音频资源。
- [ ] 三份本地 LRC 都能解析并按真实播放时间滚动、高亮。
- [ ] 播放/暂停同时控制音频、歌词、人物和场景 cue。
- [ ] 进度条与歌词基于同一个时间源，可点击或拖动进度。
- [ ] 陪跑关闭后没有好友继续留在跑道上运动；开启后有分批入场动画。
- [ ] 好友管理面板能模拟上线、下线、2 人、4 人和 8 人场景。
- [ ] 最多 8 人限制生效，用户本人不能被踢。
- [ ] 人物沿正确的同心椭圆轨迹移动，前后层级自然，无倒着跑或长期穿模。
- [ ] 三首歌曲分别能触发接力、毕业帽与左右手、相遇爱心彩蛋。
- [ ] 点击好友可查看陪伴数据并踢出。
- [ ] 周报卡可以成功生成和下载 PNG。
- [ ] 375 × 812、390 × 844 和桌面窄窗口下没有横向滚动或关键内容遮挡。
- [ ] 浏览器控制台无报错，刷新后仍能正常进入 Demo。
- [ ] `npm run build` 通过。
- [ ] `npm run preview` 下三首 MP3、三份 LRC、三张封面和唱片图片均加载成功。
- [ ] `dist/` 不包含本机绝对路径，放到域名子目录后资源路径仍然有效。

## 十二、交付方式

请直接完成项目，不只给代码片段。结束时只需汇报：

1. 实现了哪些交互。
2. 三首本地音频、LRC 的接入情况，以及是否还有需要现场微调的彩蛋时间点。
3. 开发环境与生产构建预览的启动命令和地址。
4. 你实际执行的构建/测试结果。

如果时间不足，优先级依次为：

1. 播放器与三首歌切换
2. 唱片素材、封面与正确跑步轨迹
3. 陪跑开关和好友管理面板
4. 三首歌的关键彩蛋
5. 分享周报
6. 好友详情与踢出
7. 模拟邀请和留言预留
